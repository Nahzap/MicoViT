# --- Núcleo común (Fases A, B, C y CLI) ---
numpy>=1.26,<2.3
pandas>=2.2,<2.3
pyarrow>=15.0
pyyaml>=6.0
rich>=13.7
typer>=0.12,<0.13
tqdm>=4.66

# --- Procesamiento de imagen ---
opencv-python>=4.9
Pillow>=10.2
# Opcional (E): decode streaming sin cargar RGB completo — pip install pyvips + libvips
# pyvips>=2.2.0
scikit-image>=0.22
imagecodecs>=2024.1

# --- Visualización (capas L0–L10) ---
matplotlib>=3.8
seaborn>=0.13
PyQt5>=5.15

# --- ML / Stage1+Stage2 (Fases D, E) ---
# torch / torchvision se instalan aparte para fijar CUDA o CPU explícitamente.
h5py>=3.10
scikit-learn>=1.4
einops>=0.7
transformers>=4.41

# --- Frecuencia / wavelets (rama C) ---
PyWavelets>=1.5
scipy>=1.11

# --- Calidad y métricas ---
torchmetrics>=1.3 ; python_version < "3.12"

# --- Self-training / experiments ---
hydra-core>=1.3
omegaconf>=2.3

# --- Dev / Test ---
pytest>=8.0
black>=24.0
ruff>=0.4
